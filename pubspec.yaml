name: my_video_player_app # Changed from video_player to my_video_player_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter

  # Add video_player package here
  video_player: ^2.9.1

  cupertino_icons: ^1.0.8
  flick_video_player: ^0.9.0
  visibility_detector: ^0.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - images/
